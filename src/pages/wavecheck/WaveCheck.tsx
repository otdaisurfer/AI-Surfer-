// Canonical Wave Check implementation.
//
// The public /wave-check route uses the full Wave Audit experience in RouterApp.
// Keep this module aligned so repository-indexed documentation does not describe
// the retired three-question readiness quiz as the live product.
export { default } from "../wave-audit/WaveAudit";
